{
  "name": "diary-book",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:templates && npm run check:derive && npm run check:cover && npm run check:store && tsc --noEmit && vite build",
    "build:templates": "node scripts/build-templates.mjs",
    "check:derive": "node scripts/check-derive.mjs",
    "check:cover": "node scripts/check-cover-art.mjs",
    "check:store": "node scripts/check-store.mjs",
    "verify:threeui": "node scripts/verify-threeui.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  }
}
